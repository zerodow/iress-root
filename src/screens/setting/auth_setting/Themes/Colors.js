// @flow
import { Platform } from 'react-native'

const colors = {
  transparent: 'rgba(0,0,0,0)',
  black: 'black',
  blue: '#20c2d8',
  atBackgroundColor: 'rgba(255, 255, 255, 0.9)'
}

export default colors
