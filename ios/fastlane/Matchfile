git_url("https://gitlab.com/phong.dang/certificates.git")
git_branch("equixv4")

storage_mode("git")
clone_branch_directly(true)

type("development") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier(["com.quantedge.equixv4"])
# username("phong.dang@quant-edge.com") # Your Apple Developer Portal username
shallow_clone(true)
force_for_new_devices(true)
# ENV["FASTLANE_PASSWORD"] = "quantedge"
# ENV["MATCH_PASSWORD"] = "quantedge"

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options

# The docs are available on https://docs.fastlane.tools/actions/match
